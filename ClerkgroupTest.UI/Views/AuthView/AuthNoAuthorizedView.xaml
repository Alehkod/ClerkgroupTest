<UserControl x:Class="ClerkgroupTest.UI.Views.AuthView.AuthNoAuthorizedView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ClerkgroupTest.UI.Views.AuthView" 
             xmlns:components="clr-namespace:PasswordBoxMVVM.Components;assembly=PasswordBoxMVVM" xmlns:views="clr-namespace:ClerkgroupTest.UI.ViewModels.Views" d:DataContext="{d:DesignInstance Type=views:AuthNoAuthorizedViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBox>Login:</TextBox>
            <TextBox Text="{Binding UserName}"></TextBox>
        </StackPanel>
        
        <StackPanel Grid.Row="1">
            <TextBox>Password:</TextBox>
            <components:BindablePasswordBox Password="{Binding Password}"/>
        </StackPanel>

        <Button Grid.Row="2" Content="Login" Command="{Binding LoginCommand}"/>
    </Grid>
</UserControl>
