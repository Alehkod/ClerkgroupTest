<UserControl x:Class="ClerkgroupTest.UI.Components.AuthView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ClerkgroupTest.UI.Components"
             xmlns:views="clr-namespace:ClerkgroupTest.UI.Views.AuthView"
             xmlns:viewmodels="clr-namespace:ClerkgroupTest.UI.ViewModels.Views" 
             xmlns:componentmodels="clr-namespace:ClerkgroupTest.UI.ViewModels.Components" 
             d:DataContext="{d:DesignInstance Type=componentmodels:AuthViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.Resources>
            <DataTemplate DataType="{x:Type viewmodels:AuthAuthorizedViewModel}">
                <views:AuthAuthorizedView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type viewmodels:AuthNoAuthorizedViewModel}">
                <views:AuthNoAuthorizedView/>
            </DataTemplate>
        </Grid.Resources>

        <ContentControl Content="{Binding CurrentViewModel}"/>
    </Grid>
</UserControl>
